# Sigma Intelligence Platform — Day 12 Lab
# Install before class: pip install -r requirements.txt

boto3>=1.34.0
python-dotenv>=1.0.0
snowflake-connector-python>=3.6.0
pandas>=2.0.0
langfuse>=2.0.0          # optional — observability dashboard; skip if not using
streamlit>=1.0.0         # optional — for building a simple UI; skip if not using
